{
  "name": "console-experiment",
  "version": "1.0.0",
  "description": "A game experiment with the JS console",
  "main": "index.html",
  "scripts": {
    "start": "live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Technohacker",
  "license": "MIT",
  "dependencies": {
    "normalizecss": "^3.0.0"
  },
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
